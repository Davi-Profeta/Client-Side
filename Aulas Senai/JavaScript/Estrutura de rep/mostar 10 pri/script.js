/* Crie um programa que exiba os 10 primeiros
 números ímpares positivos usando a estrutura for. */
let num;

for(let i=1 ; i <=20 ; i++){
    if(num = i % 2 != 0){
        window.alert(i)
    }
}