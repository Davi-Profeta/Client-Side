/* Crie um programa que mostre os números de 10 até 1 (de forma decrescente)
, usando a estrutura while. */
let contador = 10
while(contador >= 1){

    window.alert(contador)
    contador--;
}