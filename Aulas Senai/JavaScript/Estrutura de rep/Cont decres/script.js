/*Crie um programa que faz contagem decrescente de um número escrito 
pelo usuário, utilizando a estrutura for. */
let num = Number(window.prompt("Digite um número"))

for(num ; num>=1 ; num--){
    window.alert(num)
}
window.alert("BOOM!!")